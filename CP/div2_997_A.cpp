#include<bits/stdc++.h>
using namespace std;
#define long long int
int main(){
    int t;
    cin >> t;
    while ( t-- ){
        int n, side;
        cin >> n >> side;
        vector<pair<int,int>> vp;
        for ( int i = 0; i < n ; i ++ ){
            int x, y;
            cin >> x >> y;
            vp.push_back({x, y});
        }

        for ( int i = 1; i < n ; i ++ ){
           for ( i t j = 0 ;j < n ; j ++ ){
            int 
           }
        }
    }g
    hey i'm samt your brother i will be availabble for all the avaabhnjdn thenbde tr
}fsdfhrtrgtgfsf