#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(){
    int n, k;
    cin >> n >> k;
    string s;
    cin >> s;
    int cnt0 = count(s.begin(), s.end(), '0');
    int cnt1 = n - cnt0;

    int maxGood = cnt0 / 2 + cnt1 / 2;
    if(k <)
}
int main(){
     int t;
     cin >> t;
     while(t--) solve();
}