#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

#define debug(x) cout << #x << " = " << x << "\n";
#define vdebug(a) cout << #a << " = "; for(auto x: a) cout << x << " "; cout << "\n";


void solve() {
    // int n, m;
    // cin >> n;
   
    // vector<ll> a(n);
 
    // for (ll &x : a) cin >> x;

    // vector<int> cnt(6, 0);
    // int f = 0;
    
    // for(int i = 0; i < n; i++){
    //    if(a[i] == 1 || a[i] == 0 || a[i] == 2 || a[i] == 3 || a[i] == 5)
    //        cnt[a[i]] ++;
       
    //    if(cnt[0] >= 3 && cnt[1] >= 1 && cnt[2] >= 2 && cnt[3] >= 1 && cnt[5] >= 1){
    //     f = i + 1;
    //     break;
    //    }
    // }

    // cout << f << endl;
    // string s;
    // cin >> s;
    double a = 36.104198, b = -115.172279;
    cout << fixed << setprecision(6) << a << " " << b << endl;
  
    
   
    
   
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    
    int t = 1;
    // cin >> t;
    while (t--) solve();
    
    return 0;
}
